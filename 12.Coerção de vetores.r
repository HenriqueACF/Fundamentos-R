#Coerção de vetores
numeros <- c(1:3)
class(numeros)
#atribuindo novos valores
numeros <- c(numeros, 4.4)
numeros
class(numeros)
#Vetor só pode ter um tipo de dado!
#Adicionar uma string no vetor
numeros <- c(numeros,"18")
numeros
class(numeros)

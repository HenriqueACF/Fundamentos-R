#Criando matrizes
# Vetores != Matrizes
#Vetores são elementos em uma linha com varias colunas
v <- c(1:10)
v
#Matrizes são varias linhas com variais colunas
#matrix(data, [quantidade de linhas], [quantidade de colunas])
matrix(c(1:100)) #1 coluna e 100 linhas
matrix(c(1:100),  5) #5 linhas e 20 colunas
#Atribuindo uma matriz ha uma variavel
m <- matrix(c(1:100),  5)
m
